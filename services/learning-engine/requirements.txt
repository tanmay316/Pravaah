firebase-admin>=6.0
pydantic>=2.0
python-dotenv>=1.0
google-generativeai>=0.8.0
groq>=0.11.0
httpx>=0.27.0
# litellm is the optional last-resort provider fallback only (see _get_litellm in worker.py).
# It costs ~200MB RSS on import, so leave it out of memory-constrained deployments.
# litellm>=1.40
