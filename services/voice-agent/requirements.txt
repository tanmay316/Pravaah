# Realtime voice agent only. Keep this list minimal: everything here is loaded into the
# process that has to keep audio flowing.
#
# Deliberately excluded:
#   turn-detector  - pulls transformers + an ONNX model; VAD-based endpointing is used instead
#   litellm        - ~200MB RSS on import, and the agent talks to Groq/Gemini directly
#   kokoro-onnx    - only needed by the standalone kokoro_server.py TTS container
livekit-agents[google,groq,openai,silero]~=1.5
python-dotenv>=1.0
google-generativeai>=0.8.0
websockets>=12.0
